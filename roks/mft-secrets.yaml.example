apiVersion: v1
kind: Secret
metadata:
  name: mft-secrets
type: Opaque
stringData:
  dbUrl: jdbc:wm:${DB_TYPE}://${DB_SERVER}:${DB_PORT};DatabaseName=${DB_NAME};EncryptionMethod=SSL;ValidateServerCertificate=true;HostNameInCertificate=${DB_SERVER};TrustStore=/certs/db-truststore.jks;TrustStorePassword=${DB_TRUSTSTORE_PASSWORD}
  dbUser: 
  dbPassword: 
  adminPassword: 

---

apiVersion: v1
kind: Secret
metadata:
  name: db-truststore
type: Opaque
data:
  db-truststore.jks: 